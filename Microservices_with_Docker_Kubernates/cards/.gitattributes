# Automatically detect text files and normalize line endings
* text=auto

# Force LF line endings for Java, JavaScript, HTML, and other source files
*.java text eol=lf
*.js text eol=lf
*.html text eol=lf
*.css text eol=lf
*.properties text eol=lf
*.xml text eol=lf
*.txt text eol=lf
*.sh text eol=lf

# Force CRLF line endings for Windows batch scripts and executable files
/mvnw text eol=lf         # Maven wrapper script (LF, as it's often used across platforms)
/mvnw.cmd text eol=crlf    # Windows batch file for Maven (CRLF)
/*.cmd text eol=crlf       # Any other .cmd files (CRLF)
/*.bat text eol=crlf       # Batch files (CRLF)
